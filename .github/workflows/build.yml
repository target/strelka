name: "Build Strelka On Push"
on:
  push
jobs:
  strelka:
    name: "Build Strelka On Push"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Builds Strelka
        run: docker-compose -f build/docker-compose.yaml build
